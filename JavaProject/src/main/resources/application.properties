spring.application.name=marketplace

# Database Configuration (PostgreSQL)
spring.datasource.url=${DATABASE_URL:postgresql://postgres_wwez_user:M2IfoZnl4hCNzuiqwibBPYYDTJNTIvCn@dpg-d4t9pevdiees73av4l80-a/postgres_wwez}
spring.datasource.username=${DB_USERNAME:postgres_wwez_user}
spring.datasource.password=${DB_PASSWORD:M2IfoZnl4hCNzuiqwibBPYYDTJNTIvCn}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.h2.console.enabled=true

# Server
server.port=${PORT:8080}

# Thymeleaf
spring.thymeleaf.cache=false

# Messages
spring.messages.basename=messages
spring.messages.encoding=UTF-8
